export enum DomainEntity {
  SERIE = 'serie',
  ACTOR = 'actor',
  NEW = 'new',
  AWARD = 'award',
  CHAPTER = 'chapter'
}

export enum Resource {
  series = '/series',
  serie = '/series/:id',
  actors = '/actors',
  actor = '/actors/:id'
}
